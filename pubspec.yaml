name: riverpod_statenotifier_freezed_article
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

  #State Management
  flutter_riverpod: ^0.13.1+1
  freezed_annotation: ^0.14.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  freezed: ^0.14.1+1
  build_runner: null

flutter:
  uses-material-design: true
